

## MANUAL
Cloudflare deploy convention for future projects: use Wrangler token auth via environment/secret rather than browser login, resolve account_id from the active Cloudflare dashboard session when needed, and do not persist raw API tokens in project files or AGENTS.md.
